box: node
build:
  steps:
    - npm-install
    - script:
        name: gulp build
        code: ./node_modules/.bin/gulp build
deploy:
  steps:
    - lukevivier/gh-pages@0.2.1:
        token: $GIT_TOKEN
        domain: adam-harper.com
        basedir: build
